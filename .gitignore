__pycache__/

.ipynb_checkpoints

profile_default/
ipython_config.py

.python-version

.env
.env.backup
.env.local
.env.*.local
.venv
env/
venv/

# TLS certs/keys (mkcert-generated local dev certs, e.g. localhost.pem)
*.pem
*.key
*.crt

.vscode/
.idea/
*.swp
*.swo

.DS_Store
Thumbs.db

# Models and Data
# Anchored to the repo root: an unanchored `models/` also matches src/models/,
# which is the L3 layer package (DraftEngineDesign.md §9.0) and must be tracked.
/models/
*.pkl
# local HTTP cache (FFC/CFBD) — regenerable, not source
data/

.agents/
.claude/*
CLAUDE.md
.pytest_cache/

# live-draft save files (event log per draft)
draft_state_*.json

notes/
# web frontend
web/node_modules/
web/.next/
web/.env.local

# Yahoo OAuth token cache (probe + live client)
.yahoo_token.json
