# Loose ranges here; requirements.lock pins the exact validated versions
# (pip install -r requirements.lock to reproduce a known-good environment).
# Core data + modeling (DrafterSpec.md §4.10)
nflreadpy
pandas
pyarrow
numpy
scipy
scikit-learn
sqlalchemy>=2.0
psycopg2
python-dotenv

# Config + gradient-boosted quantile model
pyyaml
lightgbm

# Resilient third-party HTTP (CFBD + FFC) via src/ingest/http.py
httpx

# NOTE: CFBD college data is pulled via its REST API through ingest/http.py, NOT the
# `cfbd` Python client — that client pins pydantic v1, which conflicts with
# nflreadpy's pydantic v2 requirement. New .env key: CFBD_API_KEY (CFBD is not
# anonymous), alongside DATABASE_URL.

# xgboost was the retired weekly model; LightGBM quantile GBM replaces it.

# HTTP API (api/) — draft sessions, players, recommendations. httpx (client
# for the API's own tests) already listed above as a shared ingest dependency.
fastapi
uvicorn[standard]
