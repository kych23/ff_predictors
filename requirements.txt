# Loose ranges here; requirements.lock pins the exact validated versions
# (pip install -r requirements.lock to reproduce a known-good environment).

# Core data + modeling
nflreadpy
pandas
pyarrow          # pandas parquet engine — the draft-night bundle format
numpy
scipy
scikit-learn

# Config + gradient-boosted quantile model
pyyaml
lightgbm

# Resilient third-party HTTP (FFC ADP, local narration backend)
httpx

# Yahoo OAuth probes under scripts/spikes/ read credentials from .env
python-dotenv

# Identity spine, §11.4 `fuzzy` cascade tier. NOT optional: both spine.py and
# match.py guard the import so the tiers above fuzzy keep working without it,
# and that guard means an absent rapidfuzz degrades SILENTLY — every name the
# deterministic tier misses goes straight to the confirm card. Found exactly
# that way: the fuzzy tier was dead in this environment until it was tested.
rapidfuzz

# Narration (§18). Optional at runtime — `narrate()` falls back to
# `render_table`, which needs no network, no key and no model and states the
# same attribution record. The default backend is a LOCAL model over Ollama
# (no package needed, plain HTTP via httpx); `anthropic` is only the hosted
# fallback. Listed because strategy.yaml ships `narration.enabled: true`, so an
# absent package is a silent downgrade rather than a chosen one.
anthropic

# REMOVED with the v1 deletion: sqlalchemy + psycopg2 (the research/serving
# Postgres split), fastapi + uvicorn[standard] (the api/ package). The v2
# engine reads a parquet bundle and writes a local SQLite ledger; it opens no
# database connection and serves no HTTP.
